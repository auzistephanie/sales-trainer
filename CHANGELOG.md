# 改版歷史

- **2026-08-04**：**ATS「話我知我幫你加返」由得個提示變成真係做嘢** **背景**：Stephanie 喺 Telegram 見到 ATS 分數訊息話「上面呢啲如果你實際有經驗，話我知我幫你加返」，照打咗自己識嘅技能，回覆完全冇反應——bot 淨係跌返去通用「輸入 /practice」fallback。查 `api/webhook.py`／`interview_trainer.py` 發現呢句提示由頭到尾冇對應嘅 session state，純粹一句冇兌現嘅文案。**做咗**：① `interview_trainer.py`：`generate_tailored_cv_content()` 加 optional param `confirmed_extras`，有值就加一條 prompt 規則畀 AI 將呢啲字眼自然編入 summary/core_competencies/bullet，但唔准作未提過嘅公司/職銜/日期/數字；新增 `match_confirmed_skills(text, missing)`——simple substring 比對（唔叫 API），揪出用戶回覆入面提到邊啲 missing keyword。② `api/webhook.py`：`handle_job_tailored_cv()` / `handle_jd_tailored_cv()` 計完 ATS 若有 `missing` keyword，就存 session state `ats_followup`（job flow，用 `interview_session` key）／`jd_ats_followup`（JD-session flow，加落 `interview_jd_session` 度），帶埋 missing list + 沿用嗰組 keyword。`handle_message()` 加兩段對應處理：回覆完全冇 match 中任何 missing keyword → 列返個清單畀用戶睇實際字眼再試（唔清 state，可以重試）；有 match 到 → 重新生成 tailored CV（帶 `confirmed_extras`）→ 新 docx 上傳 Drive → 用返同一組 keyword 重計 ATS → 回覆 before/after 分數對比 → 清 state。共用 helper `_ats_followup_regenerate()` / `_send_ats_followup_result()`。**核實**：`ast.parse` 兩檔過；用假 send_telegram/upload_to_drive/generate_tailored_cv_content/calculate_ats_score/Redis 函數 mock 咗 3 個情景（job flow 命中／job flow 完全落空／JD-session flow 命中）跑 `handle_message()`，確認 state 轉移、before/after 分數、`confirmed_extras` 傳遞全部啱（見對話紀錄）。⚠️ 未做——sandbox 冇真 Upstash Redis/Telegram token/DeepSeek key，未喺真 Telegram 度打一次實際回覆核實；push 後要 Stephanie 親自喺 bot 度試一次先算完全確認。

- **2026-08-04**：**新增 `/clearjobs`（刪晒求職記錄，需確認）+ 修 ATS 分數空 JD 誤導問題** **背景**：Stephanie 用 Streamlit CRM（求職_CRM.py）睇返自己啲申請，發現全部卡片都顯示 `ATS 86`，懷疑計錯。查 code 發現 `calculate_ats_score()` 本身係每份工獨立計（`job.get("jd","")` + 呢份工 tailored 之後嘅 CV 文本），冇 aliasing/寫錯 job 嘅 bug；但 `/addjob` 手動流程嗰步「貼 JD 或 /skip」如果撳咗 /skip，`job["jd"]` 就係空字串——DeepSeek 憑空 JD 都要氹 15-20 個 keyword 出嚟，temperature 0.3 下多次都氹到差唔多嘅通用詞，對住同一份固定原版 CV 計，分數自然黐埋近似值，唔係計錯，係「冇餵嘢就冧到同一個數」。**修**：① `handle_job_tailored_cv()`（`api/webhook.py`）加 `if job.get("jd","").strip()` 判斷，JD 空白就完全跳過 ATS 計算 + 唔存 `ats_score`，改發一句提示「呢份工冇貼 JD，跳過 ATS 分數」；有 JD 嘅工唔受影響。② 新增 `/clearjobs` 指令——`handle_clearjobs_start()` 先問「真係要刪晒 N 份記錄？」，撳「🗑️ 確認刪晒」（`clearjobs_confirm` callback）先真正 `save_jobs([])`，撳取消或唔理都唔會刪，防手滑。`/help` 同 `set_webhook` 嘅 command menu 同步加返呢個指令。**核實**：`py_compile api/webhook.py` 過；⚠️ 未做——push 後要探訪 `/api/set_webhook` sync menu，再由 Stephanie 喺 Telegram 實際撳 `/clearjobs` → 確認 → 見到清空，先算完成（本 sandbox 冇 Upstash Redis／Telegram bot token，唔可以代跑）。

- 2026-08-01（承 07-31 制度複檢）：**`scripts/github_push.py` 修靜默故障** — 舊版 `_PUSH_STATE_DIR` 用 `os.path.dirname(REPO)` 當 stephanie-personal 係隔籬 folder；04-MAINTENANCE §6 將 5 個 repo 搬出 Drive Mirror 後假設崩咗，`makedirs` 靜靜咁喺 `~/Desktop/dev`、`~/dev`、`daily-novel/` 開咗 3 個假 stephanie-personal，concurrent-push 偵測對 6 個 repo 死咗都冇人知（真 state 檔停留喺 7/26–7/30）。改為 `STEPHANIE_PERSONAL_DIR` 環境變數 → Drive 正本絕對路徑 → legacy sibling 三段 resolve，搵唔到就**唔寫兼出聲**（S5「死咗邊個會知」）。12 份 script 一齊改，py_compile 全過，sales-trainer 實跑驗證真 state 有更新。假 folder 已收入 `_to_delete/`。

- 2026-07-31：`.gitignore` 加 `*.bak-*` 第二道防線 — 配合 06-STANDARDS §S3「備份一律開喺 `_to_delete/`」，就算漏咗 mv 都唔會畀 `github_push.py` 誤推上 GitHub（2026-07-25 事故嘅根治）。本 repo 冇 governance `backups/`，所以唔需要 negation 例外。

- 2026-07-31：`CLAUDE.md` ⚙️ Standards block 事實更正 — `governance 00–05（派 subagent 先讀 01+03…）` → `governance 00–06（派工跟 01 §1 門檻表，要派先抄 03 模板…）`。06-STANDARDS.md 07-18 已加入但 router 行仲寫 00–05；「先讀 01+03」07-31 鬆綁（為派一個 agent 先燒 230 行本身係 context 稅）。正本改動見 stephanie-personal CHANGELOG 同日條目。

- 2026-07-31：repo 搬出 Google Drive Mirror，新位置 `~/Desktop/dev/sales-trainer`。原因：node_modules 嘅 npm `.bin` symlink 令 Drive 持續報 sync error。換機唔再靠 Drive 帶 secrets，詳見 `stephanie-personal/docs/NEW-MACHINE-SETUP.md` §9。

> 唔會自動讀入每次對話 context，需要時先 `read_file`。

- **2026-07-28**（landing page Gate A/B 雙選擇 + hero 視覺對比）：**Web App／Telegram bot 由文字提及變成有畫面對比嘅雙選擇** **背景**：跟手上一輪 CTA fix 之後，Stephanie 覺得成頁仲係「成日 focus 落 bot」——主要係 hero 果個大手機 mockup 淨係得 Telegram chat 畫面，落到最尾先見到 Web App 呢個選擇，用家由頭嗰下感覺就係「呢個係個 Telegram 工具」。**做咗**：① Gate section 由單一「攞面試通行證」boarding pass 改做兩張並排卡——Gate A · Web App（連 `interview-coach-ten-delta.vercel.app`）／Gate B · Telegram bot（連 `t.me/salestraineraubot`，之前一直未加落 landing page），每張卡加返細 mockup 縮圖幫用家一眼睇出分別（A：瀏覽器地址欄 + 已練題數/平均分/連勝日 stat + CV Health/ATS/Tracker chip；B：迷你 chat bubble）。② Hero 大 mockup 由單一 phone chat 改做「瀏覽器視窗（Web App）+ phone chat（Telegram）」並排砌，各自貼「🌐 Web App」／「💬 Telegram」標籤，第一屏就見到兩種畫面。③ 全頁文案過一次：hero eyebrow/sub、STOP03 三步上手、jobkit「一站到底」總結卡（原本仲寫「全部喺同一個 bot」，同新框架自相矛盾）、footer，全部改做 channel-neutral，兩邊都提到。④ Gate 加返一句老實 caveat：「兩邊資料獨立、唔會同步，建議揀定一種持續用」（Stephanie 揀咗要加）。**修 bug（Stephanie 截圖發現）**：第一版 hero mockup 用負 offset 疊埋兩張卡，喺窄啲嘅桌面闊度（~1000-1150px）會撞到左側標題文字，「Web App」個 badge 仲畀 `.browser` 自己嘅 `overflow:hidden` 剪咗一截得返「We」——起因係個 badge 本身係 `.browser` 嘅子元素，用負 top/right 想「浮出」個卡個角，但個卡自己有 overflow:hidden（用嚟剪返個深綠色 header bar 嘅圓角），連自己個仔都一齊剪埋。**修法**：badge 搬做 `.browser` 嘅 sibling（擺喺 `.hero__visual` 度），offset 全部收窄（-58px→-20px），加一條 901-1150px 嘅中等闊度 breakpoint 進一步收窄，避免撞正文字。**核實**：`div`/`section` 開合標籤數過一次（112/112、5/5）；push 後等 Vercel rebuild，`claude-in-chrome` 喺 1568px 闊度截圖核實冇再遮擋/撞字，Gate A/B 兩粒掣真係撳過——A 開新分頁去 `interview-coach-ten-delta.vercel.app`、B 開新分頁去 Telegram `@salestraineraubot`，兩個都啱。⚠️ 未做：冇試到 901-1150px 呢個中等闊度 breakpoint 本身嘅實際渲染（瀏覽器工具嘅 resize_window 呢次冇成功縮細個視窗，淨係喺 1568px 度截圖驗證咗），淨係靠 CSS 數學計過應該安全，未肉眼睇實過；手機 (<900px) 嘅 stacked 版都未截圖驗證。

- **2026-07-28**（Web App 追近 Telegram bot 功能，第一批）：**Cowork interview 發現 web app 同 bot 功能唔對齊，補返 3 樣** **背景**：跟上一輪 landing page 對話發現，bot 有但 web app 冇嘅嘢包括「度身 CV + Cover Letter」「每日貼士」「7 日 follow-up 提醒」（自動搵工推送/週報因為 `daily_check.py` 而家係 single-user 假設，做齊多用戶版係另一截工程，呢期未做，已同 Stephanie 講清楚）。**做咗**：① `webapp/api/index.py` 加 `POST /api/app/cv/tailor`，直接駁 `interview_trainer.py` 已有嘅 `generate_tailored_cv_content` / `generate_cover_letter_from_jd` / `build_cv_docx` / `build_cover_letter_docx`（AI 邏輯一行冇改，2026-07-25 已 mirror 落 webapp/api/_lib），CV+Cover Letter 兩份 .docx base64 一齊喺 JSON 返，前端新增 `TailorCvTool` 畫面（上載/貼 CV + JD → 落載兩個 .docx）。② Home 畫面加每日貼士卡，call 現成但之前冇畫面用嘅 `api.tip()`。③ Home 加 follow-up 提醒 banner，client-side 由 `coach_jobs` 計算（狀態「已投」/「面試中」+ `created_at` 超過 7 日），撳落去去到工作追蹤。**限制**：follow-up banner 用 `created_at` 做近似值，唔似 bot 版有獨立 `last_touch` 隨互動更新咁精準——Supabase `coach_jobs` 表冇呢個欄，如果之後想要更準，要加返呢個 timestamp 欄先。**核實**：3 個檔案 `esbuild`/`py_compile` 語法檢查全過；push 後用真瀏覽器登入 web app 實測——貼士卡顯示真內容、新工具卡「度身 CV + Cover Letter」出現；真實填 CV+JD 生成，8 秒內完成，兩個落載掣都撳咗，冇 console error。⚠️ 未做：follow-up banner 冇試過「有嘢顯示」嗰個 case（冇辦法喺 UI 整到 7 日前嘅測試數據），淨係confirm 咗「0 份工唔會顯示」呢個負向 case；真正見到條 banner 出現要等 Stephanie 有 job 擺咗 7 日先自然驗到。

- **2026-07-28**：**`landing.html` 主 CTA 一直冇連去真 app（Cowork interview 發現）** **問題**：全部 CTA（nav「免費開始」、hero「🚀 免費開始練習」×2、「GO ▸」、footer）一律 `href="#contact"`——即係跳返頁面自己嗰個 section，唔係去 Telegram bot 或 web app。footer 仲留低一句未填嘅 placeholder「填返你嘅 Telegram bot 連結」，明顯係漏咗手尾。**核實方法**：本機 + 已部署嘅 `sales-trainer-wheat.vercel.app/landing.html` 兩份一致，證實唔係本機獨有問題。 **修**：跟 Stephanie 拍板「主掣連 web app，web app 自己再連 Telegram bot」——5 個 CTA 全部改連 `https://interview-coach-ten-delta.vercel.app`（`target="_blank"`），Telegram 連結（`t.me/salestraineraubot`）冇加返落 landing page。 **同時**：「求職功能組」(#jobkit) 補返兩個一直有做但冇喺 landing page 提過嘅功能——「度身 CV + Cover Letter」（貼 JD 連結觸發）同「求職 Tracker」（`/addjob` `/listjobs`，狀態追蹤 + 7 日冇郁 follow-up 提醒），卡數由 5 張加到 7 張，標題同 lead 文字同步改。 **核實**：push 後等 Vercel rebuild，`WebFetch` 核實 live 版 5 個 CTA 已全部指向 web app、7 張卡標題正確；再用真瀏覽器（claude-in-chrome）由 landing page 撳「免費開始」，確認開新分頁去到 `interview-coach-ten-delta.vercel.app` 首頁，畫面正常顯示已練題數/平均分/連勝日 + 求職工具箱（CV Health/薪酬情報/ATS 等），非空白頁。⚠️ 未做：未逐張新卡對住手機窄屏 (`max-width:600px`) 核實排版，7 張喺 3-col grid 尾行剩 1 張，理論上 CSS 會自動 reflow，但未截圖驗證。

- **2026-07-25**（ATS 第二輪，同日 `e2f7d30` 之後）：**Keyword 抽咗行政資料當技能考 + 訊息重複 + 詞根對唔上** **實測發現**（EngSeeds Programme Executive 職位，65/100）：① 23 個 keyword 入面有 3 個係**招聘啟事嘅行政資料**——`EngSeeds Programme`（僱主專屬 programme 品牌名）、`1-year contract`、`5-Day Work`。冇任何 CV 應該寫呢啲，但佢哋佔咗分母，永遠 match 唔到，無端端扣 13 分。② `⚠️ JD 要求但你 CV 未有（8 個）` 已經列曬，`💡` 嗰行又再抄頭 5 個，純冗餘。③ `_ats_stem("coordinate")` → `"coordinate"` 但 `"coordinated"` → `"coordinat"`，兩者對唔上（今次靠 substring 僥倖中）；`-ion`／`-ment` 詞尾一律唔剪。 **修**：① keyword prompt 加 **INCLUDE ONLY**（hard skills／tools／qualifications／domain terms）＋ **EXCLUDE**（僱主專屬 programme 名、合約長度、工時安排、公司名、地點、薪酬、generic filler 如 "team player"）。② `improvement` 唔再列 missing 詞，只留一句「上面呢啲如果你實際有經驗，話我知我幫你加返；冇嘅話唔會幫你作」。③ `_ats_stem` 加 `ion(s)`／`ment(s)` 詞尾、最多剪兩層（businesses → business → busines）、剪完去尾 `e`——`coordinate`／`coordinated`／`coordinating`／`coordination` 全部歸一做 `coordinat`，`management`／`managing`／`manage` 歸一做 `manag`。④ `smart_match` 嘅 multi-word 拆詞加 `len(p) > 1`，撇除 apostrophe 拆出嚟嘅單字母（`Bachelor's degree` → bachelor/**s**/degree，嗰個 `s` 令命中率 2/3 = 66% < 70% 而誤判 miss）。 **核實**：真 Mac 用**實際嗰 23 個 keyword** 重跑——剔走 3 個行政資料前 83/100，剔走後 **95/100（19/20）**；詞根歸一同 `Bachelor's degree` 誤判逐項單測過。 **真實性**：全部仍然只改「點樣量」——剔走嘅係唔應該考你嘅行政資料，唔係放寬標準；CV 內容一個字冇加。 ⚠️ 未做：未行過真 Telegram flow。

- **2026-07-25**：**ATS Match Score 一直量錯對象（12/100 成因）→ 三步修正** **問題**：`handle_job_tailored_cv()` / `handle_jd_tailored_cv()` / `bot_listener.py` 生成完 tailored CV 之後，`calculate_ats_score(jd, cv_text)` 嘅 `cv_text` **一路都係 `load_cv_text()` 攞返嚟嘅原版 CV**——啱啱生成嘅 `cv_data` 從來冇入過公式。即係話「Tailored CV 嘅 ATS 分」其實係**原版 CV 嘅分**，tailored 寫得幾好個分都唔會郁。改 prompt 完全幫唔到。 **修（3 步，全部只改「點樣量」，冇一步改 CV 內容）**：① 新增 `flatten_cv_data(cv_data)` 將 tailored dict（summary／core_competencies／所有 bullets／certs…）攤平成純文字，4 個 call site 全改用佢；同一組 keyword 靠新 `keywords=` 參數重用，分別計 tailored 同原版做 baseline（**唔會多叫一次 API**）。② 新增 `smart_match()` 取代 `k.lower() in cv_lower`——完全相等唔中就 normalize + `_ats_stem()` 詞根 + multi-word ≥70% token 命中當 match（例：JD 寫 "stakeholder management"、CV 寫 "managing stakeholders" 舊版判 0）。③ keyword 抽取 prompt 由廣東話改英文 + 明令 `Output in ENGLISH only, exactly as written in the JD`（舊版有機會回中文詞對住英文 CV 必然 0 match）；`format_ats_message` 第二個參數由 `cv_health_score` 改 `baseline_score`——舊版攞 CV Health（量結構／動詞／量化）減 ATS keyword match，係雞同鴨比，所以成日錯誤顯示「需改善 ⚠️」。 **真實性**：`generate_tailored_cv_content()` 嘅 "NEVER fabricate companies, titles, dates, metrics, skills or certifications" 原封不動；missing keyword 照顯示，訊息明寫「冇嘅話唔會幫你作」。 **核實**：真 Mac 跑 `py_compile` 4 個檔全過；用 15 個真實 keyword + 一份 sample 原版/tailored CV 實測——改前 47/100（7 個 match），改後 baseline 47 → tailored **87/100（13/15 match）**，delta +40 顯示正常。 **同步**：`webapp/api/_lib/interview_trainer.py` mirror 由 root 覆蓋（順手補返之前漏咗嘅 `build_cover_letter_docx`）；`docs/FEATURES.md` ATS 段落重寫。 ⚠️ 未做：未行過真 Telegram flow（要 Stephanie 實測 `/listjobs → 生成 Tailored CV`）。

- **2026-07-25**：**CLAUDE.md 瘦身（跟 Anthropic《new rules of context engineering for Claude 5》）** **原則**：CLAUDE.md 要輕，token 主力花喺 **gotchas**（模型預設判斷會做相反嘅嘢），能推斷／已寫喺 spec 嘅嘢一律唔好抄多次。 **本次**：5648 → 3598 bytes（63 → 43 行，−36%）。「核心檔案」逐個檔嘅描述同「Web App」段落大部分可由 code／`docs/APP_SPEC.md` 推斷，抽走；散喺各處嘅保命規則收埋做一段 Gotchas；Bot 指令清單刪走（讀 code 就知），但**保留**「加/改指令後必須跑 set_webhook」呢條規則本身。 **刻意保留（唔准當廢話刪）**：所有「反直覺」規則同「有 reason 嘅決定」——刪咗下個 session 一定會做錯。改前版本 → `_to_delete/CLAUDE.md.bak-20260725`。 **背景**：同日 audit 咗全部 14 份 CLAUDE.md，結論係**其餘 11 份唔使大改**（全部已喺 100 行/6KB 內，整體只慳 ~27%，唔值得冒刪錯 gotcha 嘅險）。只做呢 3 份高收益嘅。

- **2026-07-25**：**`.active-session.lock*` 冇入 .gitignore → session 鎖檔一直推上 GitHub** **問題**：`session-lock.sh` 喺每個 repo 根寫 `.active-session.lock`；release 嗰陣 Drive mount `rm` 唔到（device bridge 冇 rm 權限），會 fallback 改名做 `.active-session.lock.DELETE-ME-<epoch>`。兩種檔全部 repo 都**冇入 `.gitignore`**，所以 `github_push.py` 照推——最舊一個殘留檔 timestamp 係 **2026-07-14**，即係呢個洩漏行咗成十日。 **修**：12 個 repo（含 `novel-web`）`.gitignore` 全部加 `.active-session.lock*`（一條 pattern 蓋埋活鎖同 `.DELETE-ME-*`）；現存 16 個殘留檔 mv 入各自 `_to_delete/`。 **同類第三宗**：同日先修咗 ①`_to_delete/` 冇入 ignore、②`.bak-*` 冇入回收筒，今次係 ③鎖檔。三宗共通根因＝**新產生嘅暫存檔冇人幫佢配 ignore rule**。 ⚠️ **未做（要 Stephanie 拍板）**：真正治本係改 `session-lock.sh`，唔好將鎖寫入 repo 樹，改寫去 `stephanie-personal/scripts/.session-locks/<repo>.lock` 集中管——咁就冇檔會落 repo，亦唔使靠 12 份 `.gitignore` 各自記得。

- **2026-07-25**：**`github_push.py` 全 repo 統一放 `scripts/`，刪走 root 重複 copy** **問題**：`sales-trainer`／`Travel App`／`daily-novel` 各有兩份 `github_push.py`（root ＋ `scripts/`），內容唔同版本；`novel-web` 就淨得 root 一份。而 `auto_push.sh` 搵 script 係 **root 優先**，制度正本（`PUSH-SETUP.md`／`repo-setup` skill／`06-STANDARDS` §S1）**三處都寫 `<repo>/scripts/github_push.py`**——即係 daemon 同 session 各行各路，drift 咗都冇人知（同日先撞到 4 份仲留住已拆嘅 `sync_local_head()`）。 **修**：① root 嗰份 mv 入 `_to_delete/`；② `novel-web` 由 root 搬去 `scripts/`（同時換返 `scripts/` 版嘅 `REPO = dirname(dirname(__file__))`，root 版計法放錯位會指去上一層）；③ `auto_push.sh` 第 40–41 行次序反轉做 **`scripts/` 優先、root 做 fallback**；④ 各 repo CLAUDE.md 嘅 push 路徑記錄同步改（Travel App 原本寫「root 嗰份係舊 copy」——實情當時 root 先係新版，記錄一直係反嘅）。 **核實**：全 `dev/` 12 份 `github_push.py` 全部喺 `scripts/`、`REPO` 路徑計法逐個對過；`bash -n auto_push.sh` 過。 **餘下良性差異**（唔動）：`catnu-app` 多 `bootstrap_empty_repo`（空 repo 首推）、`venturenix-lab-seminar` 多 `parse_args`、`AI for elderly`／`stephanie-portfolio`／`MakeMyHome`／`fable-prompt` 只差註釋長短。

- **2026-07-25**：**`github_push.py` 版本 drift → 同步返正本，拆走 `sync_local_head()`** **問題**：`scripts/github_push.py`（root 嗰份本身已經係新版） 仲行緊舊版 script——有 `sync_local_head()`（push 完 `git fetch` + `git reset --mixed` 令 `git status` 睇落乾淨），亦冇 2026-07-15 加嘅 concurrent-push 警報。但正本喺 **2026-07-15 已經拆走 `sync_local_head()`**：`.git` 住喺 Google Drive streaming 資料夾，嗰兩個 git **寫入**指令成日撞 stale ref lock（`refs/heads/main.lock`），一卡幾十個鐘，令人誤以為「push 唔到」。呢幾個檔等於仲留住當初出事嗰條路徑。 **修**：用正本 `stephanie-personal/scripts/github_push.py` 覆蓋。 改前版本 → `_to_delete/scripts-github_push.py.bak-20260725`。 **核實**：全 `dev/` grep `sync_local_head` 已歸零；`REPO = ` 路徑計法逐個對過（`scripts/` 版用 `dirname(dirname(__file__))`，root 版用 `dirname(__file__)`，放錯位會指去上一層）。 **背景**：全 `dev/` 15 份 `github_push.py` 共 7 個版本。其餘差異屬良性（catnu-app 多 `bootstrap_empty_repo`、venturenix 多 `parse_args`、其他淨係註釋長短），今次只動有 `sync_local_head` 嗰批。 ⚠️ 提醒：`auto_push.sh` 搵 script 嘅次序係 **root 優先、`scripts/` 做 fallback**（第 40–41 行）。
- **2026-07-25**：**散喺 repo 嘅 `.bak-*` 備份推咗上 GitHub → 搬入 `_to_delete/`**——本 repo `.gitignore` 已經有 `_to_delete/`，但備份檔冇搬入去、留咗喺原位，所以 `github_push.py` 照當普通檔上傳；GitHub Git Trees API 核實 remote `main` 實際有：`CLAUDE.md.bak-20260718`（repo 根）。修法：`mv` 入 `_to_delete/`（跟全局規則「清理一律 mv 去該 project 根目錄 `_to_delete/`」），push 後 `github_push.py` 嘅 `deletions` 邏輯自動由 remote 樹刪走，14 日後由 `stephanie-personal/scripts/clean_to_delete.sh` 真刪。背景：同日先修咗 6 個 repo `.gitignore` 漏咗 `_to_delete/`，跟手全 repo 掃多次，發現呢批係另一種漏法——**回收筒 ignore 咗，但檔案根本冇入過回收筒**。⚠️ 只由 HEAD 移除，舊 commit 歷史仍然有；已 grep 過冇 token／secret 值。

- **2026-07-25**：DeepSeek 官方 2026-07-24 停用舊 model 名 `deepseek-chat`/`deepseek-reasoner`（迎代 `deepseek-v4-flash`/`deepseek-v4-pro`）——`webapp/api/_lib/interview_trainer.py`（interview_trainer.py 嘅 mirror copy，14 處呼叫）同 `api/daily_check.py`（Daily Check 自動搵工 AI 排名）跟住換做 `model="deepseek-v4-flash"` + `extra_body={"thinking":{"type":"disabled"}}`（保持原本非thinking快速回覆行為，同 root `interview_trainer.py` 同日稍早已改嘅版本一致）。已用真 Mac + 真 API key 實測（curl 式 request：status=200、回覆內容正常、`reasoning_content` 為空確認 thinking 已關）。⚠️ 未做：純機械式 model 名替換，低風險，未逐個觸發 Telegram bot／webapp 完整對話流程。 順手同步更新 `CLAUDE.md`＋`docs/APP_SPEC.md` 入面舊嘅「DeepSeek-V3」文字描述，改做 `DeepSeek（deepseek-v4-flash）`，同代碼一致。

- **2026-07-18**（第二輪）：CLAUDE.md「三條保命規則」升格做完成前檢查 DoD（加實跑驗證＋push 核實 HEAD 兩條）。改前原版＝`.bak-20260718`。
- **2026-07-18**：CLAUDE.md 重複規則段（寫入分流／Git push／AI 制度 router）換做 5 行「⚙️ Standards」block，正本 → `stephanie-personal/docs/ai-governance/06-STANDARDS.md`（skill 系統重整 session）；改前備份 `CLAUDE.md.bak-20260718`。
- **2026-07-12**：搬機至 Google Drive Mirror——canonical 路徑由 `~/sales-trainer` 改為 `~/Desktop/Stephanie-Google Drive/dev/sales-trainer`；HEAD／`git fsck`／檔案數（3206 個）核對通過；`stephanie-personal/scripts/autopush-registry.txt` 已更新新路徑；舊 `~/sales-trainer` 已加 `_MIGRATED_TO_GOOGLE_DRIVE.txt` 標記，確認後可刪。
- **2026-07-10**：`github_push.py` 修好「push 完 `git status` 仍顯示大量假改動」——script 經 GitHub API 寫 remote、唔 advance 本地 HEAD，令 status 成日同舊 HEAD 比。Fix：push 成功後加 `sync_local_head()`（帶 token fetch + `git reset --mixed` 對齊；token 唔寫檔唔 print、失敗唔阻塞、只郁 HEAD+index）；已清殘留 `.git/index.lock` 並驗證 status 歸零。
- **2026-07-02**：求職 CRM 網頁（Streamlit）UI 重做 —— 原本淨係 default 主題 + `st.expander` + emoji 分辨狀態，冇卡片感。新版：metric 卡片化、kanban 卡加狀態色帶（灰=Applied／橙=Phone Screen／紫=Interview／綠=Offer／紅=Rejected）+ status badge、申請漏斗由純文字 metric 改做 bar chart。**新增功能**：kanban 卡直接加咗「更新狀態」selectbox，寫返同一個 Redis key `interview_jobs`，同 Telegram `/listjobs → 更新狀態` 共用同一份資料、雙向同步。用 `streamlit.testing.v1.AppTest` 實測跑過 `pages/求職_CRM.py`（連真實 Upstash 資料），確認冇 exception、metric/badge/kanban/漏斗都正常渲染先報告完成
- **2026-06-23**：初版創建，銷售話術訓練，10 種拒絕類型
- **2026-06-28**：重大 pivot — 改為 AI 面試教練，新增 14 種行業、MBTI 16種 coaching、變現 credit 系統
- **2026-06-29**：新增 Job Application Tracker — /addjob, /listjobs, AI 面試問題、Key Tips、status tracking（Applied/Phone Screen/Interview/Offer/Rejected）
- **2026-07-01**：CV Health Score、HK Salary Benchmark onboarding、ATS Match Score（Tailored CV 生成後自動顯示）、`/negotiate` 薪酬談判 role-play、`/debrief` 面試後覆盤分析。`bot_listener.py` 同步補齊 CV 上傳基建（之前得 `api/webhook.py` 有）。詳見 `FEATURE_SPEC.md`
- **2026-07-01**：發現 `bot_listener.py`（local polling）同 Vercel webhook 一直喺度衝突 —— Telegram webhook 已設定，真正接單嘅係 `api/webhook.py`，`bot_listener.py` 嘅 `getUpdates` 一直畀 Telegram reset。順手發現指令 menu 淨係喺手動探訪 `/api/set_webhook` 先會更新，補做咗一次同步
- **2026-07-01**：正式決定唔用 local —— 停咗 `bot_listener.py` 嘅 LaunchAgent（搬去 `_disabled/`），Vercel webhook 做唯一 runtime
- **2026-07-01**：停用 5 次免費練習限制（Stephanie 自用）—— `check_free_limit()` 改為直接 return True，`api/webhook.py` + `bot_listener.py` 同步；`FREE_SESSION_LIMIT`/`UPGRADE_MSG` 同舊 logic 留底註解，日後想開返俾其他用戶隨時復原
- **2026-07-02**：修 JobsDB 抓取失敗 —— 根因係 `fetch_jd_via_jina()`（`api/webhook.py`）`X-No-Cache:true`（強制慢 render）+ `timeout=15`（JobsDB 冷 render 成日過 15s，實測有條要 16.2s）。改為移除 X-No-Cache、`X-Return-Format=markdown`、`timeout=45`、加一次 retry、支援 optional `JINA_API_KEY`（有就用，加速 16.2s→1.5s + 鬆 rate limit）。⚠️ **Vercel 側要喺 dashboard 加環境變數 `JINA_API_KEY` 先生效**（`.env` 只影響 local）
- **2026-07-02**：升級 CV / Cover Letter 質素 —— `generate_tailored_cv_content()` + `generate_cover_letter_from_jd()`（`interview_trainer.py`）搬入 `tailored-cv-generator` skill 嘅 CRITICAL RULES：唔准作假、鎖公司名/職稱/日期、ATS keyword tailoring、core competencies 6→8-12、bullets 3→4、summary 唔提學歷/語言、temperature 收緊（CV 0.4→0.3、CL 0.6→0.35）
- **2026-07-02**：修 CV/CL 實測 4 個問題 ——
  1. **公司/職位變鬼字**（"Skip to content @ Markdown Content"）：`handle_url_message` 之前用「Jina 頭 10 行最短句」亂估，估中咗 Jina 導航文字。新增 `clean_jd_text()`（清 Title:/URL Source:/Markdown Content:/導航）+ `extract_company_role()`（DeepSeek 乾淨抽 company/role）取代亂估
  2. **Cover letter 太長**：280 字 → ~150 字（2 段，`max_tokens` 900→500），實測 138 字
  3. **Education 印 placeholder**（"not specified in CV"）：prompt 禁止填 placeholder、搵唔到就返 `[]`；`build_cv_docx` 加 `_valid_edu()` filter，冇有效學歷就唔印個 section
  4. **Drive fallback 訊息突兀**：「（Drive 未設定，直接傳送）」→「✅ Tailored CV 已生成！」
  - ⚠️ **Google Drive 上傳**：bot（Vercel）同 CV skill（Cowork）係兩套獨立系統。bot 要上 Drive 須喺 `.env` + Vercel 加 `GOOGLE_CREDENTIALS`（service account JSON）+ `GOOGLE_DRIVE_FOLDER_ID`；未設定就直接 Telegram 傳檔（`utils.upload_to_drive` fallback）
- **2026-07-02**：開通 bot Drive 上傳 —— service account `cv-uploader@just-clover-487108-a0`、folder `1QMBJwdWBknIMVHxIDZs7i7u9oy0SQ6JY`（同 CV skill 共用）。加入 local `.env`（gitignored）。個 folder 係 **Shared Drive**，`utils.upload_to_drive` 之前冇 `supportsAllDrives=True` 會 404 File not found，已補上並實測上傳成功。⚠️ **Vercel dashboard 要自己加 `GOOGLE_CREDENTIALS`（單行 JSON）+ `GOOGLE_DRIVE_FOLDER_ID` 先會喺正式 bot 生效**
